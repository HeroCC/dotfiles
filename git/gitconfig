[user]
	name = HeroCC
	email = HeroCC@users.noreply.github.com

[github]
	user = HeroCC

[core]
	excludesfile = ~/.gitignore_global

[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
